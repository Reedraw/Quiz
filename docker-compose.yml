version: '3'

services:
  app:
    build:
      context: .
    image: dmodena/kahoot_clone_nodejs
    ports:
      - '80:3000'
    networks:
      - kahoot_clone_net
  mongodb:
    image: mongo
    networks:
      - kahoot_clone_net

networks:
  kahoot_clone_net:
    driver: bridge
